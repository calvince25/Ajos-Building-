
  # Exact Design Replica

  This is a code bundle for Exact Design Replica. The original project is available at https://www.figma.com/design/NYsGAfTa6f6q4KGpelSTCk/Exact-Design-Replica.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  